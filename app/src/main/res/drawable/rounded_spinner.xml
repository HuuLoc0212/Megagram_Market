<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Màu nền -->
    <corners android:radius="12dp" /> <!-- Bán kính bo tròn -->
    <stroke
        android:width="1dp"
        android:color="@color/black" /> <!-- Viền (nếu cần) -->
</shape>